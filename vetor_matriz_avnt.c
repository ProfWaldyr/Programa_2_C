#include <stdio.h>

int main() {
    int matriz [3] [3];
    // Declaração matriz 3x3 inteiros;

    // inicialização de estrutura de repetição "FOR" aninhados, matriz 3x3;

    for (int i = 0; i < 3; i ++) {
        
        for (int j = 0; j < 3; j ++)  {

            matriz [i] [j] = i + J; // atribuindo valores a matriz
            printf("matriz [%d][%d] = %d\n", i , j, matriz [i] [j]);


        }

    }

return 0;
    }
